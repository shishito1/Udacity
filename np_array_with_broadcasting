import numpy as np

# create np array with 1s in column 1, 2s in column 2, 3s in column 3 and 4s in column 4
# using a range broadcasting will multiply each column in the ones array by each number in the arange array
X = np.ones((4, 4)) * np.arange(1, 5)

print(X)